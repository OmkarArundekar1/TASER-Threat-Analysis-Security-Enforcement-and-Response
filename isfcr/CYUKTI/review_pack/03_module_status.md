# CYUKTI — Module-by-Module Implementation Status

> Last verified: 2026-09-26. Counts reflect live Neo4j query and pytest run from this date. **New module**: `evaluation/` (independent ground-truth + evaluator framework, 7 categories, 54 tests) — status **B** (complete + tested, real preliminary results produced for 4/7 categories; see `review/paper_metrics_source_of_truth.md`), not yet **A** since no dataset has been human-reviewed/locked.

Status codes: **A** = Complete + validated (tests + live evidence) · **B** = Complete + partially validated (tests only, or live but not both) · **C** = Implemented, not experimentally validated · **D** = In progress · **E** = Planned · **F** = Blocked

| Module | Status | Evidence | Inputs | Outputs | Algorithm/Tech | Validation | Metrics |
|---|---|---|---|---|---|---|---|
| Wazuh Listener (`listener/wazuh_listener.py`) | **A** | Live PID 10228 running, offset=400886=alerts.json size, 391 alerts processed since project start | `alerts.json` | queued alerts | offset-poll, thread queue | Live traffic + manual restart drills (Phase 20D-F) | 0 backlog; 1 known non-fatal recurring exception (see `10_limitations_and_future_work.md`) |
| MITRE Resolver (`mitre_resolver.py`) | **A** | 15 unit tests (`test_mitre_resolver.py`), 8 integration tests, live-validated on real alerts | raw alert | `MitreResolution` | rule precedence | Unit + integration + live (Phase 20C/F, re-verified 2026-09-25 post rule-fix/reboot) | Current: 120 live alerts, 26 NATIVE_WAZUH (21.7%), 94 UNKNOWN (78.3%), 0 REVIEWED/INFERENCE/AMBIGUOUS. (Earlier 2026-08-31 snapshot: 391 alerts, 39 NATIVE_WAZUH, 352 UNKNOWN.) |
| Reviewed Mapping Registry (`mitre_rule_registry.py`) | **E** (deliberately empty) | File exists, 0 entries by design | Wazuh rule ID | technique_ids | dict lookup | Tested (empty-registry behavior) | 0 rules mapped |
| Deterministic Inference | **E** (infrastructure only) | `DETERMINISTIC_INFERENCE_RULES = []` | alert | technique_ids | rule-callable list | Tested (mechanism only) | 0 live rules |
| Unattributed Event Path (`create_unattributed_attack_event`) | **A** | Live: 5 real UNKNOWN AttackEvents inspected in Neo4j, all invariants confirmed | resolved-UNKNOWN alert | AttackEvent (no attack_id) | Cypher CREATE, no Technique MERGE | Unit + integration + live | tps=0 confirmed on all 5; 0 fabricated attack_id |
| Neo4j Client (`neo4j_client.py`) | **A** | Central to every other module; 111 Campaigns/212 AttackEvents/858 Technique live (2,539 total nodes, 20,804 total relationships, 2026-09-25) | Python calls | Cypher writes/reads | neo4j driver, bolt | Exercised continuously | 0 orphaned AttackEvents as of the 2026-09-12 check (124/124 linked); not re-run at the current 212-event scale |
| MITRE→Stage Mapping (`mitre_mapper.py`) | **B** | 8 tests (`test_mitre_mapper.py`), audited against real STIX tactics | technique_id | risk stage | static dict, 13 entries | Unit tested + manually STIX-verified | 1 known gap: T1548.003 absent (see Part K) |
| STIX/ATT&CK Import (`mitre_import/`) | **A** | 858 real Technique nodes live in Neo4j, vendored STIX v19.1 (25,843 objects, 53MB) | STIX JSON | Neo4j Technique/Tactic/Malware/Tool/ThreatActor nodes | JSON parsing, Cypher import | Live graph inspection | 858 Technique nodes confirmed |
| Campaign Reconstruction (`campaign_reconstruction.py`) | **A** | 10 tests, fixed 44 real orphaned events into 27 reconstructed campaigns (historical), `check_integrity()` clean as of the last check | orphaned AttackEvents | Campaign nodes (MERGE-based, idempotent) | deterministic, evidence-only reconstruction | Unit + live repair verified | 0 orphans as of 2026-09-12 (124/124 linked); not re-run at the current 212-event scale |
| Risk Scoring (`risk_scoring.py`) | **A** | 6 tests, single source of truth for normalize/level thresholds | raw TPS | normalized score, level | linear normalize + fixed thresholds | Unit tested | TPS_CEILING=1500 (unchanged) |
| Risk Recalculation (`risk_recalculation.py`) | **A** | 4 tests proving idempotency | (attack_id, occurrences) | recomputed risk_score | deterministic full recompute | Unit tested | Verified idempotent |
| Attack-Chain Learning (`chain_updater.py`) | **B** | Live: 3 real NEXT_TECHNIQUE edges exist; no dedicated unit test file for this module specifically (covered indirectly) | consecutive real techniques | NEXT_TECHNIQUE edges | frequency counting | Live graph only | 3 edges: T1110.001→T1110 (6), T1110→T1078 (5), T1078→T1110.001 (1, below MIN_TRANSITION_OBSERVATIONS) |
| Prediction Engine (`prediction_engine.py`) | **B** | `predict_next_readonly()` covered by 4 tests in `test_next_technique_pipeline.py`; live NEXT_TECHNIQUE graph exercised | technique | predicted next + confidence | top-1 frequency, thresholded | Unit tested; **not** benchmarked for accuracy as a standalone system | See Phase 18 result: 4/12 correct on evaluable real transitions (33%) — explicitly ruled `INSUFFICIENT_FOR_SUPERVISED_ML` |
| Next-Technique Label Pipeline (`ml/label_generator.py`) | **A** | 6 tests, corrected in Phase 18, live-verified via dataset rebuild | technique sequence | next_technique, prediction_correct (NA-aware) | per-transition evaluation | Unit tested + real dataset rebuild | 20/60 rows have next_technique; 12/20 evaluable; 4 correct/8 incorrect |
| Campaign Correlation / Operation (`campaign_correlation_engine.py`, `operation_manager.py`) | **C** | Code present, imported and called in `process_alert()`; 50 Operation nodes exist live (2026-09-25, was 43 on 2026-09-12) | campaign features | matched/created Operation | similarity scoring | No dedicated test file found; live nodes exist but matching quality not evaluated | Operation count = 50 (existence only, not accuracy) |
| Threat Attribution (`threat_attribution_engine.py`) | **C** | Code present; `attribution_collector.py` has 2 tests (empty-actors, decay-ranking) as part of evidence-collector suite | Campaign context | ranked actor candidates | historical similarity by attacker_ip | Unit tested at the evidence-collector level only; no attribution-accuracy dataset exists | NOT MEASURED (no ground-truth actor-identity dataset in repo) |
| Evidence Collectors (`evidence/collectors/*.py`) | **A** | 13 tests in `test_evidence.py` covering all 6 collector types + store dedup/conflict logic | raw signals (MITRE, graph, detection, CTI, history, attribution) | `Evidence` objects | per-source normalization | Unit tested | 6 evidence-source categories confirmed |
| Evidence Orchestrator (`evidence/orchestrator.py`) | **B** | 1 integration test (`test_orchestrator_assembles_evidence_from_every_source`) | attack_id/campaign | assembled evidence list | orchestration of 6 collectors | Unit tested only | N/A |
| Investigation Loop / Confidence Architecture (`investigation/*.py`) | **B** | 25 tests in `test_investigation.py`, covers entropy, coverage, stopping, conflict handling | CampaignContext + optional model | `InvestigationState`, `ConfidenceEstimate` | evidence-value scoring + entropy-gated confidence | Extensively unit tested; **not** re-run against a fresh live campaign during this specific audit session | N/A this session (prior session reported real XGBoost probabilities for 3 named campaigns — not independently re-verified this session, see caveat in `04_results_and_metrics.md`) |
| ML — XGBoost severity model (`ml/train_xgboost.py`) | **B** | Real trained artifact (`xgb_severity.json`, 314KB, dated Aug 28), 6 tests in `test_ml_pipeline.py`, real in-sample evaluation run this session | 57 features | severity class + probabilities | gradient-boosted trees | Unit tested (synthetic + real pipeline mechanics); in-sample metrics computed this session (see Part E/F) — **no held-out split exists** | accuracy=0.917 (in-sample, n=60, NOT a generalization estimate) |
| ML — SSL Autoencoder (`ml/ssl_pipeline.py`) | **A** | Real trained artifact (`autoencoder_best.pth`, 455KB), 2 tests, real CICIDS2017-derived training data (`window_features.npy`, 52MB, 21,176 windows per prior-session record) | network flow windows | reconstruction error / anomaly score | autoencoder (PyTorch) | Unit tested (rejects too-few-windows, produces artifacts and converges) | Convergence confirmed by test; no held-out anomaly-detection benchmark in repo |
| ML — SSFT (`ml/ssft.py`) | **B** | Real artifact (`ssft_dataset.npz`, 2.5MB), 2 tests (round-trip, requires-trained-model) | autoencoder output | latent + reconstruction-error dataset | learned transform via trained encoder | Unit tested | N/A |
| ML — GNN (`ml/gnn/`) | **B** | 11 tests covering encoder, SAGEConv layer, model forward/gradients, end-to-end train+reload | campaign graph | node embeddings | hand-implemented GraphSAGE (no torch_geometric) | Unit tested on synthetic graphs only — **no real-campaign GNN benchmark exists** | NOT MEASURED on real data |
| RAG Retriever (`rag/mitre_retriever.py`) | **A** | 7 tests including "indexes hundreds of real techniques" against the real STIX corpus | text query | ranked ATT&CK documents + relevance | TF-IDF | Unit tested against real corpus | Indexes real technique count (hundreds; exact count not re-extracted this session) |
| MISP Integration (`misp_sync.py`, `cti_publisher.py`, `misp_event_generator.py`) | **C** | Code present, `CTIPublisher` initializes successfully (confirmed in live listener logs) | resolved incident | MISP event (if configured) | REST API client | No dedicated test file found; live publish success **not confirmed** — `MISP_API_KEY` was empty as of Phase 19 (not re-checked this session) | NOT MEASURED |
| Dashboard API (`dashboard_api.py`) | **B** | 7 tests (`test_dashboard_api.py`) + 3 campaign-context tests; live-callable | HTTP | JSON | Flask | Unit tested; not load-tested | N/A |
| React Dashboard (`frontend/`) | **B** | Source present (27 mounted components + 1 orphaned, `PredictionPanel.tsx`), builds cleanly, 109 tests across 17 files (2026-09-25 — was 18 components/no tests as of the earlier snapshot) | API JSON | rendered UI | React/TS/Vite | Automated frontend test suite now exists; not load-tested | 109/109 passing |
| Dataset Generation (`ml/dataset_builder.py`, `scripts/build_real_dataset.py`) | **A** | Frozen real dataset: 60 campaigns, 66 columns, rebuilt and verified across Phases 16-18 | Neo4j campaigns | `campaign_dataset.csv` | deterministic feature extraction | Extensively tested + real-data rebuilds | 60 rows confirmed live this session |
| Dataset Validity Gate (Phase 17 analysis) | **A** (analysis complete) | `scripts/phase17_dataset_validity_gate.py`, full report produced | frozen dataset | validity report | statistical audit (no ML) | N/A (analysis, not a model) | Formal verdict: `NOT_READY_FOR_CALIBRATION` |
| Audit Logging | **D** | `logs/prerana_listener.log` exists but is 0 bytes in current environment (stdout captures the same content; file-handler flush behavior unconfirmed) | listener events | log file | Python `logging` | Not validated as a durable audit trail | File observed empty despite active logging to stdout |
| Infrastructure (Wazuh/Neo4j/MISP/Docker) | **B** | Live-verified: Wazuh manager/indexer/dashboard active (systemctl), Neo4j bolt reachable, MISP container healthy | — | — | native services + Docker | Live health checks (Phase 19-20) | See `08_current_live_state` in Part H |
| Attack-Generation / Lab Components | **C** | Kali/Ubuntu VMs exist and were live-verified reachable (Phase 19); no automated attack-execution scripts exist in the repo (Atomic Red Team not integrated) | — | — | manual VM-based execution | Manual, one smoke-test telemetry check performed | No automated red-team harness present |
| Documentation | **B** | Extensive in-session phase reports exist as conversation history; this `review_pack/` is the first consolidated on-disk documentation artifact | — | markdown | — | N/A | N/A |
